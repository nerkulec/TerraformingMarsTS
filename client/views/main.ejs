<%- include('top_bar') %>
<div class="content">
    <div class="container">
<% if(!session.user){ %>
    
        <div class="row justify-content-center">
            <div class="flex-column col-8 d-flex">
                <div class="login-panel">
                    <h4>Login: </h4>
                    <form action="/login" method="post">
                        <div class="form-group row align-items-center">
                            <label for="nameInput" class="col-2 m-0">Username: </label>
                            <input type="text" name="name" id="nameInput" class="col-8 form-control"/>
                        </div>
                        <div class="form-group row align-items-center">
                            <label for="passwdInput" class="col-2 m-0">Password: </label>
                            <input type="password" name="password" id="passwdInput" class="col-8 form-control"/>
                        </div>
                        <div class="form-group row align-items-center">
                            <input type="submit" value="Login" class="col-5 mx-auto btn btn-success">
                        </div>
                        <h5>You don't have an account yet?</h5>
                        <a href="/register">
                            <button type="button" class="btn btn-primary">Register</button>
                        </a>
                    </form>
                </div>
                <div class="guest-panel">
                    <h4>Enter as guest: </h4>
                    <form action="/guest" method="post">
                        <div class="form-group row align-items-center">
                            <label for="guestNameInput" class="col-2 m-0">Nick: </label>
                            <input type="text" name="name" id="guestNameInput" class="col-8 form-control"/>
                        </div>
                        <div class="form-group row align-items-center">
                            <input type="submit" value="Join" class="col-5 mx-auto btn btn-primary">
                        </div>
                    </form>
                </div>
            </div>
            
        </div>
    </div>
    
<% } else{ %>


    <div class="row justify-content-center">
        <div class="flex-column col-8 d-flex align-items-center">
            <a href="#">
                <button class="btn btn-info">CREATE ROOM</button>
            </a>
            <hr class="w-100">

            <form class="row w-100 mb-5">
                <div class="col-12 d-flex justify-content-between">
                    <span>Unranked</span>
                    <span class="mr-3">Auto</span>
                    <span>Ranked</span>
                </div>
                <div class="col-12 form-group">
                    <input type="range" class="custom-range" min="0" max="2" id="ranked">
                </div>
                <div class="col-6 form-group w-50 text-center">
                    <label for="min_elo">Min. Elo</label>
                    <input type="number" class="form-control w-50 m-auto" id="min_elo">
                </div>
                <div class="col-6 form-group w-50 text-center">
                    <label for="max_elo">Max. Elo</label>
                    <input type="number" class="form-control w-50 m-auto" id="max_elo">
                </div>
                <div class="form-group m-auto">
                    <input type="submit" class="btn btn-success" value="Find more">
                </div>
            </form>
            
            <div class="list-group w-100">
                <% for(room of rooms){ %>
                    <div class="row d-flex list-group-item list-group-item-action mx-0 p-0 border-0 room-box">
                        <a href="#" class="col-9 d-flex w-100 h-100 px-3 py-3 align-items-center room-details" id="<%= room.room_id %>">
                            <div class="col-3"><%= room.name %></div>
                            <div class="col-2"><%= room.ranked %></div>
                            <div class="col-2"><%= room.elo_range %></div>
                            <div class="col-2"><%= room.players %>/<%= room.max_players %></div>  
                        </a>
                        <div class="col-3 room-creator-name d-flex align-items-center justify-content-center">
                            by &nbsp;<a href="/user/<%= room.creator_id %>"><%= room.creator %></a>
                        </div>
                    </div>
                <% } %>
            </div>
            <a href="/room/<%= room.room_id %>" class="btn btn-success w-50 mt-2">Join Room</a>

            <!--
                nazwa, kto stworzyl, ilosc graczy, czy ranked, elo-zakres(jezeli jest)
            -->
        </div>
    </div>

<% } %>
<!-- 
    Wejście:
    rooms = Room[1-20]

    Room = {
        name: string,
        room_id: int,
        creator: string,
        creator_id: int, // klikanie w nazwe gracza przenosi na /user/creator_id
        mode: string,
        players: int,
        max_players: int, // wyświetlać players/max_players
        elo_range: undefined or int[2], // albo nic albo dwie liczby np [1500, 1800], wyświetlić 1500-1800
        ranked: boolean
    }

    Dla zalogowanych:
    - u góry "stwórz pokój"
    - niżej lista pokoi w postaci listy ładnych prostokątów, klikanie zaznacza pokój (podświetla się)
    - na dole jest przycisk "join room" - przenosi na /room/room_id
    - wyżej filtry - suwaki i pola do wpisywania - jak narazie: 
                                                    - suwak ranked: [nieranked, obojetne, ranked]
                                                    - min elo
                                                    - max elo
                                                    - przycisk "find more" (stronka się przeładuje, do session musisz wrzucić wszystkie dane z filtra - najlepiej w obiekcie o nazwie 'filter')
    - w trakcie wybierania filtrów filtruj też widoczne pokoje (będzie ich ubywać, użytkownik kliknie "find more" i się przeładuje)
-->

    </div>
</div>